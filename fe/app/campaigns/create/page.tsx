export default function CreateCampaignPage() {
  return (
    <main className="shell compact">
      <h1>Create Campaign</h1>
      <p>Campaign creation form will submit to `POST /api/campaigns`.</p>
    </main>
  );
}
